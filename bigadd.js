// We need to sum big numbers and we require your help.

// Write a function that returns the sum of two numbers. The input numbers are strings and the 
// function must return a string.

// Example

function add(a, b) {
    return (Number(a) + Number(b)).toString(); 
  }
console.log(add('6382', '90'))